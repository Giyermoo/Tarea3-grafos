#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "processor.h"

// Funciones auxiliares mínimas para evitar errores
Grafo* crear_grafo_desde_archivo(const char* nombre_archivo) {
    // Por ahora devuelve un puntero vacío para compilar sin errores
    return NULL;
}

void encontrar_ruta_minima(Grafo* grafo, const char* origen, const char* destino) {
    printf("Función de búsqueda aún no implementada.\n");
}

void liberar_grafo(Grafo* grafo) {
    // Liberación de memoria (vacía por ahora)
}
